# **РОБОТА З ЕЛЕКТРОННОЮ ПОШТОЮ В PYTHON**

## **ЗМІСТ**

1. **ВСТУП**  
2. **РОЗДІЛ 1\. ЯК ПРАЦЮЄ ЕЛЕКТРОННА ПОШТА (ТЕОРІЯ)**  
   * 1.1. Історія: хто вигадав "собачку" @?  
   * 1.2. Архітектура: як лист подорожує інтернетом.  
   * 1.3. Мови пошти: SMTP, IMAP та чому API краще.  
3. **РОЗДІЛ 2\. БЕЗПЕКА: ЧОМУ ПАРОЛІ — ЦЕ МИНУЛЕ**  
   * 2.1. Проблема логіна та пароля (Basic Auth).  
   * 2.2. OAuth 2.0: вхід без передачі ключів.  
4. **РОЗДІЛ 3\. ПРАКТИКУМ: СТВОРЮЄМО TELEGRAM-БОТА ДЛЯ GMAIL**  
   * 3.1. Інструменти: що нам знадобиться.  
   * 3.2. Крок 1: База даних (Пам'ять бота).  
   * 3.3. Крок 2: "Слух" і "Голос" (Gmail API).  
   * 3.4. Крок 3: Інтерфейс (Telegram API).  
5. **РОЗДІЛ 4\. ІНСТРУКЦІЯ КОРИСТУВАЧА**  
6. **ВИСНОВКИ**  
7. **СПИСОК ВИКОРИСТАНИХ ДЖЕРЕЛ**

## **ВСТУП**

Уміння програмно працювати з електронною поштою — це одна з ключових навичок сучасного розробника. Електронна пошта (E-mail) залишається головним транспортом для офіційних документів, сповіщень від серверів, підтверджень реєстрацій та бізнес-лістування.

Часто виникають задачі, які неможливо вирішити звичайним поштовим клієнтом:

* Відправити 1000 листів з індивідуальним зверненням.  
* Миттєво реагувати на листи з темою "Терміново".  
* Створити зручного чат-бота, щоб керувати поштою з телефону, не відкриваючи важкі додатки.

Цей реферат побудований у форматі **підручника**. Ми пройдемо шлях від розуміння того, як працюють поштові сервери, до написання власного працюючого Telegram-бота мовою Python, який вміє читати та відправляти листи через Gmail.

## **РОЗДІЛ 1\. ЯК ПРАЦЮЄ ЕЛЕКТРОННА ПОШТА (ТЕОРІЯ)**

### **1.1. Історія: хто вигадав "собачку" @?**

Електронна пошта старша за сам Інтернет. Перші системи обміну повідомленнями працювали в межах одного мейнфрейма (величезного комп'ютера) ще у 1965 році.

Але справжня революція сталася у 1971 році. Інженер Рей Томлінсон писав програму для мережі ARPANET і шукав спосіб вказати, що повідомлення адресоване користувачеві на *іншому* комп'ютері. Він подивився на клавіатуру телетайпу і обрав символ @ (at), який перекладається як "на". Формат user@host (користувач НА комп'ютері) став світовим стандартом.

Сьогодні пошта — це глобальна децентралізована мережа. Коли ви відправляєте лист, він не летить напряму отримувачу, а проходить через ланцюжок серверів.

### **1.2. Архітектура: як лист подорожує інтернетом**

Уявіть, що ви (Клієнт А) пишете листа другу (Клієнт Б). Цей процес обслуговують три типи програм:

1. MUA (Mail User Agent) — "Секретар".  
   Це програма, яку ви бачите на екрані (Outlook, веб\-сайт Gmail або наш майбутній бот). Вона допомагає скласти лист і натиснути "Відправити".  
2. MTA (Mail Transfer Agent) — "Листоноша".  
   Це сервер, який займається пересилкою. Ваш "секретар" передає лист вашому "листоноші". Той дивиться на адресу (наприклад, @ukr.net), знаходить в інтернеті "листоношу" компанії Ukr.net і передає лист йому.  
3. MDA (Mail Delivery Agent) — "Скринька".  
   Коли лист дійшов до сервера отримувача, MDA кладе його у персональну папку (скриньку) користувача, де він лежить, поки його не прочитають.

### **1.3. Мови пошти: SMTP, IMAP та чому API краще**

Щоб комп'ютери розуміли один одного, вони використовують протоколи (набори правил):

#### **SMTP (Simple Mail Transfer Protocol)**

Це протокол **відправки**. Він працює тільки в один бік: від вас до сервера, або від сервера до сервера.

* Уявіть, що ви кидаєте лист у поштову скриньку на вулиці. Ви не можете залізти туди і забрати його назад або подивитися інші листи. Так працює SMTP.

#### **IMAP (Internet Message Access Protocol)**

Це протокол читання. Раніше використовувався POP3, який просто завантажував листи на комп'ютер і видаляв їх з сервера. Це було незручно.  
IMAP дозволяє керувати листами прямо на сервері. Якщо ви прочитали лист з телефону, він стане "прочитаним" і на комп'ютері.

#### **HTTP API (REST) — Сучасний вибір**

Написання програми, яка напряму спілкується мовою SMTP/IMAP, — це складно і часто повільно. Сучасні сервіси (Gmail) пропонують API (Application Programming Interface).  
Замість складних текстових команд ми просто надсилаємо HTTP-запит (як відкриття веб\-сторінки) і отримуємо дані у зручному форматі JSON.  
Переваги API: Швидкість, надійність та безпека. Саме цей метод ми використаємо у практичній частині.

## **РОЗДІЛ 2\. БЕЗПЕКА: ЧОМУ ПАРОЛІ — ЦЕ МИНУЛЕ**

### **2.1. Проблема логіна та пароля (Basic Auth)**

Найпростіший спосіб увійти в пошту — передати логін і пароль.  
Але для програміста це поганий шлях.

1. Вам доведеться зберігати пароль у коді програми (password \= "12345"). Якщо хтось побачить код — він вкраде вашу пошту.  
2. Передаючи пароль боту, ви даєте йому повний доступ до всього Google-аккаунту (фото, диск, YouTube). Це занадто ризиковано.  
3. Google блокує такі спроби, називаючи їх "Less Secure Apps".

### **2.2. OAuth 2.0: вхід без передачі ключів**

Ми використаємо стандарт **OAuth 2.0**. Це та сама технологія, яка працює, коли ви натискаєте "Увійти через Google" на різних сайтах.

**Як це працює:**

1. Наш бот каже Google: "Привіт, я хочу лише читати і відправляти листи".  
2. Google питає у вас (користувача): "Дозволити цьому боту доступ до пошти?".  
3. Ви кажете "Так".  
4. Google видає боту спеціальний файл — **Токен**.

**Токен** — це як електронний ключ-картка в готелі. Він відкриває тільки ваші двері і діє обмежений час. Навіть якщо хакер вкраде токен, він не дізнається ваш справжній пароль, і ви можете будь-якої миті анулювати цей ключ.

## **РОЗДІЛ 3\. ПРАКТИКУМ: СТВОРЮЄМО TELEGRAM-БОТА ДЛЯ GMAIL**

У цій частині ми розглянемо, як програмно реалізувати власного поштового клієнта.

### **3.1. Інструменти: що нам знадобиться**

Ми будемо писати мовою **Python**, оскільки вона має найкращі бібліотеки для цього:

* google-api-python-client — офіційний інструмент від Google.  
* python-telegram-bot — для створення інтерфейсу в месенджері.  
* sqlite3 — вбудована база даних для збереження історії.

### **3.2. Крок 1: База даних (Пам'ять бота)**

Ми не хочемо "смикати" сервери Google кожного разу, коли відкриваємо список листів. Це повільно. Тому ми створимо локальну копію (кеш).

Ми використовуємо **SQLite**, тому що вона не вимагає встановлення окремого сервера — це просто файл.

**Приклад коду (db\_manager.py):**

import sqlite3

def init\_db():  
    """Створюємо таблицю для зберігання листів"""  
    conn \= sqlite3.connect("emails.db")  
    cursor \= conn.cursor()  
      
    \# Створюємо поля: ID, Відправник, Тема, Текст  
    cursor.execute('''  
        CREATE TABLE IF NOT EXISTS emails (  
            id INTEGER PRIMARY KEY AUTOINCREMENT,  
            gmail\_id TEXT UNIQUE,  
            sender TEXT,  
            subject TEXT,  
            body TEXT,  
            received\_date TIMESTAMP  
        )  
    ''')  
    conn.commit()  
    conn.close()

### **3.3. Крок 2: "Слух" і "Голос" (Gmail API)**

Цей модуль відповідає за зв'язок із зовнішнім світом.

Читання листів:  
Листи в інтернеті мають складну структуру (MIME). Вони можуть містити і простий текст, і HTML (кольоровий текст), і картинки. Наша задача — знайти саме текст.  
**Приклад коду (Парсинг тексту):**

def get\_full\_message\_text(service, msg\_id):  
    \# Завантажуємо "сирий" лист з Google  
    message \= service.users().messages().get(userId='me', id=msg\_id).execute()  
      
    \# Шукаємо текстову частину серед вкладень  
    payload \= message\['payload'\]  
    if 'parts' in payload:  
        for part in payload\['parts'\]:  
            if part\['mimeType'\] \== 'text/plain':  
                data \= part\['body'\]\['data'\]  
                break  
    else:  
        data \= payload\['body'\]\['data'\]

    \# Декодуємо текст (він приходить зашифрованим у Base64)  
    return base64.urlsafe\_b64decode(data).decode('utf-8')

### **3.4. Крок 3: Інтерфейс (Telegram API)**

Найцікавіша частина — зробити так, щоб користувачу було зручно.  
Ми створюємо меню з кнопками: \[📥 Вхідні\] та \[✍️ Написати листа\].  
**Як працює читання:**

1. Користувач тисне "Вхідні".  
2. Бот завантажує 5 останніх заголовків.  
3. Під повідомленням бот малює кнопки з цифрами: \[1\], \[2\], \[3\].  
4. Якщо натиснути \[1\], бот покаже повний текст першого листа.

Як працює відправка (Діалог):  
Ми використовуємо "Машину станів" (ConversationHandler). Бот веде користувача за руку:

* Крок 1: Введіть Email \-\> (бот чекає текст)  
* Крок 2: Введіть Тему \-\> (бот чекає текст)  
* Крок 3: Введіть Повідомлення \-\> (бот відправляє лист)

## **РОЗДІЛ 4\. ІНСТРУКЦІЯ КОРИСТУВАЧА**

Щоб запустити цього бота самостійно, потрібно виконати 3 кроки налаштування.

**1\. Отримання "перепустки" від Google:**

* Зайдіть на сайт **Google Cloud Console**.  
* Створіть проєкт і увімкніть **Gmail API**.  
* Завантажте файл credentials.json. Це файл, який каже Google, що ваш бот — це офіційна програма.

**2\. Реєстрація бота в Telegram:**

* Знайдіть в Telegram користувача @BotFather.  
* Напишіть команду /newbot.  
* Отримайте токен (довгий рядок символів).

**3\. Запуск:**

* Встановіть бібліотеки: pip install \-r requirements.txt.  
* Запустіть скрипт: python main.py.  
* При першому запуску відкриється браузер. Увійдіть у свій Gmail і натисніть "Дозволити".

## **ВИСНОВКИ**

У цьому посібнику ми пройшли шлях від теорії до практики і створили власного поштового клієнта.

Ми з'ясували, що:

1. **API зручніше за IMAP:** Воно дозволяє працювати швидше і безпечніше.  
2. **OAuth 2.0 — це стандарт:** Зберігати паролі в коді не можна, треба використовувати токени.  
3. **Python \+ Telegram — потужна зв'язка:** Всього за 100-200 рядків коду можна створити інструмент, який замінює важкі поштові програми на телефоні.

Такий бот може стати основою для складніших проєктів: наприклад, системи техпідтримки, яка автоматично сортує скарги клієнтів, або особистого асистента, що нагадує про неоплачені рахунки, які прийшли на пошту.

## **СПИСОК ВИКОРИСТАНИХ ДЖЕРЕЛ**

1. RFC 5321 (Протокол SMTP) — *tools.ietf.org/html/rfc5321*  
2. Документація Gmail API — *https://www.google.com/search?q=developers.google.com/gmail/api*  
3. Документація бібліотеки python-telegram-bot.  
4. Саммерфілд М. "Програмування на Python 3\. Докладний посібник".